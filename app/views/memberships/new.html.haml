- page_title _('Add user')

.page-header
  %h1
    = icon('user-plus')
    = _('Add user')

= simple_form_for @user, url: memberships_path, html: { class: 'form-horizontal' } do |f|
  = f.input :username, hint: _('Student or employee account')
  = f.actions do
    = f.button :submit
